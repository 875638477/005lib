# ESP32-S31 三电机 FOC 舵机系统设计记录

## 1. 文档目的与设计边界

本文记录基于 ESP32-S31 的三轴一体化伺服控制方案。当前假设如下：

- 同时控制 3 台三相无刷直流电机（BLDC/PMSM）。
- 每台电机使用三相半桥，共需要 6 路带死区的互补 PWM。
- 每台电机使用两相电流采样，第三相电流通过 `Ic = -(Ia + Ib)` 重构。
- 每台电机轴上安装一只正交编码器（A/B，可选 Z）或绝对式磁编码器，用于 FOC 转子角度和电机速度闭环。
- 每台电机经过约 150:1～200:1 的减速箱。
- 每个减速输出轴安装一只 SPI 或 I²C 绝对式磁编码器，用于最终输出位置闭环。
- 目标是低速、大转矩、可保持位置的舵机式控制系统。

ESP32-S31 当前公开资料仍标记为预发布版本。本文依据 2026-09-11 可获得的 ESP32-S31 Series Datasheet v0.5、ESP-IDF 文档和 ESP-IDF 主线源码整理；正式硬件定版前应重新核对最新版数据手册、芯片勘误和所用封装的引脚表。

## 2. ESP32-S31 关键硬件资源

### 2.1 MCPWM

ESP32-S31 集成 4 个独立 MCPWM 控制器：MCPWM0～MCPWM3。

每个 MCPWM 控制器包含：

- 3 个 PWM 定时器；
- 3 个 PWM 操作器；
- 每个操作器包含 A/B 两个 PWM 发生器输出；
- 6 路 PWM 输出，即 3 对 A/B 输出；
- 独立的预分频器、故障处理模块、ETM 模块和捕获模块。

全芯片合计：

- 12 个 PWM 定时器；
- 12 个 PWM 操作器；
- 24 路 MCPWM 输出；
- 12 对可配置为中心对齐、互补并插入死区的输出。

同一操作器的 A/B 输出使用同一个定时器时间基准，因此两路频率相同，但可分别配置发生器动作和比较值。按一组 MCPWM 驱动一台三相电机，可同时驱动 4 台三相电机；本方案使用其中 3 组，保留 1 组备用。

### 2.2 PCNT 正交解码

ESP32-S31 的 PCNT 底层资源为：

- 2 个 PCNT 硬件实例；
- 每个实例包含 4 个独立计数单元；
- 每个计数单元包含 2 个通道；
- 全芯片合计 8 个独立计数单元。

一个计数单元的两个通道可在硬件中解码一组正交 A/B 信号。因此三台电机只需要 3 个 PCNT 单元，资源充足。Z 相可接普通 GPIO 中断，也可根据归零策略连接到相应计数单元的清零输入。

需要根据编码器参数校核最高输入边沿频率：

```text
边沿频率 = 电机转速(rpm) / 60 × 编码器PPR × 4
```

其中 `×4` 表示对 A/B 两相的上升沿和下降沿进行四倍频计数。输入滤波阈值不能设置得过大，否则高速时可能丢计数。

### 2.3 ADC

ESP32-S31 包含：

- 2 个 12 位 SAR ADC；
- 每个 ADC 最多 8 个模拟输入；
- 合计最多 16 个模拟输入；
- ADC1 对应 GPIO42～GPIO49；
- ADC2 对应 GPIO50～GPIO57；
- 支持单次采样、多通道采样、GDMA 和 ETM 相关事件/任务。

三台电机采用两相采样时，共需要 6 路相电流输入。通道数量足够，但芯片只有两个 ADC 转换器，不能把 6 路相电流在完全相同的时刻全部转换。

建议：

1. 每台电机的一路电流接 ADC1，另一路接 ADC2，使同一台电机的两相电流可由两个 ADC 并行采样。
2. 三台电机的中心对齐 PWM 载波错开相位，避免三个采样窗口重叠。
3. 在每台电机 PWM 周期内电流稳定、开关噪声较低的位置触发采样。
4. 使用 ETM、ADC 连续模式和 GDMA 降低中断延迟；具体触发链路应以实际使用的 ESP-IDF 版本验证。
5. 如果实测无法保证采样窗口、采样吞吐或噪声指标，应改用外部同步 ADC、带采样保持的电流检测方案，或重新设计 PWM/ADC 调度。

除 6 路相电流外，还应预留母线电压、功率级温度等检测通道。

### 2.4 LEDC

芯片另有 2 个独立 LED PWM 控制器，每个控制器 8 路，共 16 路普通 PWM。LEDC 可用于风扇、状态灯或其他辅助执行器，不建议代替 MCPWM 驱动三相功率桥。

## 3. 建议的资源分配

以下仅为逻辑分配示例，最终 GPIO 必须结合具体芯片封装、模组引脚、启动配置脚以及其他外设重新核对。

| 资源 | 电机 1 | 电机 2 | 电机 3 | 备用 |
|---|---|---|---|---|
| 三相 PWM | MCPWM0，3 对 A/B | MCPWM1，3 对 A/B | MCPWM2，3 对 A/B | MCPWM3 |
| 电机轴 A/B | PCNT0 Unit0 | PCNT0 Unit1 | PCNT0 Unit2 | 其余 5 个 PCNT Unit |
| Z 相 | GPIO/PCNT 清零输入 | GPIO/PCNT 清零输入 | GPIO/PCNT 清零输入 | — |
| 相电流 1 | ADC1_CH0（示例） | ADC1_CH1（示例） | ADC1_CH2（示例） | ADC1 其他通道 |
| 相电流 2 | ADC2_CH0（示例） | ADC2_CH1（示例） | ADC2_CH2（示例） | ADC2 其他通道 |
| 输出轴编码器 | SPI CS0 | SPI CS1 | SPI CS2 | 共用 SCLK/MISO/MOSI |
| 功率级故障 | MCPWM0 Fault | MCPWM1 Fault | MCPWM2 Fault | MCPWM3 Fault |

三台电机的基础引脚需求约为：

- 18 路 PWM；
- 6 路相电流 ADC；
- 6 路编码器 A/B；
- 可选 3 路 Z 相；
- SPI 共用数据/时钟线和 3 路片选；
- 另外还需要驱动器使能、故障、母线电压、温度和通信接口。

虽然 ESP32-S31 GPIO 数量较多，PCB 定义前仍必须完成逐引脚复用和电气约束表。

## 4. 推荐的三级闭环结构

```text
输出轴目标位置
       │
       ▼
输出轴位置环（输出轴绝对磁编码器）
       │ 生成电机速度或转矩目标
       ▼
电机速度环（电机轴编码器）
       │ 生成 Iq 目标
       ▼
FOC 电流环（电机轴角度 + 两相电流）
       │
       ▼
SVPWM / MCPWM → 三相逆变器 → 电机 → 减速箱 → 输出轴
```

建议的初始控制频率范围：

- FOC 电流环：10～30 kHz；
- 电机速度环：500 Hz～2 kHz；
- 输出轴位置环：100～500 Hz。

这些频率不是固定要求。最终值应根据电机电气时间常数、PWM 频率、编码器噪声、减速箱刚度、负载惯量和实测 CPU 占用确定。

FOC 使用的电角度为：

```text
θe = (极对数 × θm + 电角度零偏) mod 2π
```

其中 `θm` 必须来自电机转子侧的角度测量或可靠观测器，而不是直接来自减速箱输出轴。

## 5. 高减速比下是否需要电机轴编码器

### 5.1 高减速比不能消除 FOC 的转子角度需求

减速比为 `N` 时：

```text
电机转速 = 输出轴转速 × N
```

输出轴缓慢运动时，电机通常以更高速度运行，传感器less观测器可能在运动阶段正常工作。但舵机在保持、微动和换向时，电机仍会经过零速或极低速；此时反电动势不足，纯传感器less FOC 难以可靠估算转子角度。

### 5.2 输出轴编码器不能替代电机轴角度传感器

以 14 位输出轴编码器、200:1 减速箱为例：

```text
输出轴每转计数 = 16384
折算到每个电机机械转的计数 = 16384 / 200 ≈ 82
```

若电机有 7 对极：

```text
每个电周期计数 ≈ 82 / 7 ≈ 12
电角度分辨率 ≈ 360° / 12 ≈ 30°
```

这个分辨率不足以直接支持平顺、高效率的 FOC。实际系统还存在减速箱齿隙、扭转弹性和换向间隙：电机可能已经转过一定角度，输出轴编码器仍没有产生有效变化。

因此：

- 输出轴编码器负责最终负载位置；
- 电机轴传感器负责 FOC 电角度和电机速度；
- 两者组成双编码器伺服，可同时补偿减速误差并观察齿隙、弹性或卡滞。

### 5.3 电机轴传感器选择

按舵机适用性排序：

1. **电机轴绝对式磁编码器**  
   上电立即获得机械角度，完成电角度零偏标定后即可闭环，适合带负载启动。

2. **ABZ 增量式正交编码器**  
   分辨率和动态性能较好，但上电时 A/B 计数不包含绝对机械角度，需要执行转子定向、寻找 Z 相，或结合 UVW/霍尔换相信号。

3. **霍尔传感器加角度插值或观测器**  
   成本较低，可以实现 FOC，但零速和低速转矩波动、噪声及角度精度通常不如编码器方案。

结论是：正交输出形式并非硬性要求，但对于需要零速保持、平顺微动和高转矩效率的舵机，保留电机轴角度传感器很有价值。若结构允许，电机轴绝对磁编码器通常比单纯 ABZ 更方便。

## 6. 输出轴磁编码器接口

### 6.1 SPI 方案

三只编码器可以共用：

- SCLK；
- MISO；
- 需要时共用 MOSI；
- 每只编码器使用独立 CS。

SPI 延迟低、时序确定、没有同地址冲突，更适合多轴实时控制。应检查编码器的最大时钟、读取帧校验、磁场强度诊断和错误标志。

### 6.2 I²C 方案

I²C 可以减少片选引脚，但需要注意：

- 三只相同编码器可能使用相同且不可修改的地址；
- 地址冲突时需要 I²C 复用器、可配置地址的编码器或多个 I²C 控制器；
- 总线上的任一器件异常拉低 SDA/SCL 可能影响全部轴；
- 延迟和恢复策略通常比 SPI 更复杂。

综合考虑，三轴伺服优先选择 SPI 磁编码器。

## 7. ADC 与三电机 FOC 的重点风险

三电机方案的主要限制通常不是 PWM 数量，而是 ADC 同步采样。

必须验证：

- 两个 SAR ADC 能否在目标 PWM 频率下按计划完成三台电机的采样序列；
- 每次采样是否处于正确的导通区间；
- 电流检测放大器在采样时刻是否已经稳定；
- MOSFET 开关沿、地弹和母线纹波是否污染 ADC；
- 三台电机载波错相后是否仍能满足控制更新时限；
- Wi-Fi、蓝牙或其他高优先级中断是否造成控制抖动；
- ADC 零偏、增益和温漂是否完成校准。

推荐将每台电机的两路电流分别放在 ADC1 和 ADC2，并为三台电机安排三个错开的采样时隙。若无法得到稳定结果，不应仅靠软件滤波掩盖采样问题，应考虑外部 ADC 或调整功率级采样拓扑。

## 8. 实时软件建议

- 电流采样和 PWM 更新使用确定性的硬件事件链路。
- FOC 中断中只执行采样读取、坐标变换、PI、电压限幅和 PWM 更新。
- 不在高速控制中断中进行日志输出、动态内存分配或阻塞通信。
- 三台电机的状态、参数和故障上下文相互独立。
- 可将高速 FOC 与通信、输出位置规划分配到不同 CPU 核，但必须避免共享数据竞争。
- ESP32-S31 具有 CORDIC 加速器，可评估用于三角函数和坐标变换；是否使用应以实际 ESP-IDF 支持和基准测试为准。
- 三台 20 kHz FOC 相当于每秒约 60000 次电流环迭代。320 MHz 双核具备实现可能性，但不能仅依据主频判断，必须在启用实际通信栈和保护逻辑后测量最坏执行时间。

## 9. 安全与故障保护

每台功率级至少应具备：

- 硬件过流保护；
- 栅极驱动器故障输出；
- MCPWM Fault/Brake 立即关闭 PWM；
- 驱动器独立使能信号；
- 母线过压、欠压检测；
- MOSFET 或驱动器过温检测；
- 编码器断线、非法跳变和速度超限检测；
- ADC 饱和、偏置异常和电流不平衡检测；
- 控制任务看门狗；
- 通信丢失后的安全转矩关闭策略。

软件限流不能替代硬件逐周期过流保护。首次上电和参数调试时应使用受限电源、较低母线电压及机械限位保护。

## 10. 标定项目

每台电机至少需要保存：

- 两路电流 ADC 零偏和增益；
- 电机极对数；
- 编码器方向；
- 编码器每转计数；
- 电机电角度零偏；
- 相序和 PWM 极性；
- 减速比；
- 电机轴与输出轴方向关系；
- 输出轴机械零位；
- 最大相电流、母线电压和速度限制；
- 电流环、速度环、位置环参数。

增量式编码器上电后还需要确定初始电角度。对已连接大减速比负载的系统，上电强制对齐可能导致输出轴产生冲击，因此更适合使用绝对式电机轴编码器，或设计低转矩、安全的定向流程。

## 11. 建议验证顺序

1. 单电机、低压、开环验证三相 PWM、死区和故障关断。
2. 校准电流 ADC，验证零电流偏置和已知电流增益。
3. 验证电机轴编码器方向、计数、Z 相及最高转速下是否丢计数。
4. 标定电角度零偏，实现单电机电流环 FOC。
5. 加入电机速度环并验证零速、低速和换向性能。
6. 接入减速箱和输出轴编码器，加入外层位置环。
7. 扩展到三台电机，验证 PWM 错相与 ADC 采样调度。
8. 在通信栈全部启用的条件下测量中断延迟、最坏执行时间和控制抖动。
9. 执行过流、编码器断线、驱动器故障、堵转和通信丢失测试。
10. 最后在额定母线电压、额定负载和温升条件下整机验证。

## 12. 当前结论

ESP32-S31 的 PWM、PCNT、ADC、SPI 和 GPIO 资源可以支持以下目标架构：

- 3 台独立三相电机；
- 每台 6 路互补中心对齐 PWM；
- 每台两相电流采样 FOC；
- 每台一个电机轴角度传感器；
- 每台一个减速输出轴绝对磁编码器；
- 电流、速度、输出位置三级闭环；
- 保留第 4 组 MCPWM 作为备用。

方案的核心工程难点是三台电机的 ADC 同步采样调度、增量编码器上电初始角度、减速箱齿隙下的双编码器控制以及硬件快速保护，而不是 PWM 通道数量。

## 13. 官方参考资料

- [ESP32-S31 Series Datasheet（预发布）](https://documentation.espressif.com/esp32-s31_datasheet_en.pdf)
- [ESP32-S31 MCPWM 编程指南](https://docs.espressif.com/projects/esp-idf/en/latest/esp32s31/api-reference/peripherals/mcpwm.html)
- [ESP32-S31 PCNT 编程指南](https://docs.espressif.com/projects/esp-idf/en/latest/esp32s31/api-reference/peripherals/pcnt.html)
- [ESP32-S31 ADC 编程指南](https://docs.espressif.com/projects/esp-idf/en/latest/esp32s31/api-reference/peripherals/adc/index.html)
- [ESP32-S31 ETM 编程指南](https://docs.espressif.com/projects/esp-idf/en/latest/esp32s31/api-reference/peripherals/etm.html)
- [ESP-IDF ESP32-S31 MCPWM 资源映射源码](https://github.com/espressif/esp-idf/blob/master/components/esp_hal_mcpwm/esp32s31/mcpwm_periph.c)
- [ESP-IDF ESP32-S31 PCNT 资源映射源码](https://github.com/espressif/esp-idf/blob/master/components/esp_hal_pcnt/esp32s31/pcnt_periph.c)
- [ESP-IDF ESP32-S31 PCNT 底层定义](https://github.com/espressif/esp-idf/blob/master/components/esp_hal_pcnt/esp32s31/include/hal/pcnt_ll.h)
