# ESP32-S31 高速无刷风机控制选型记录

## 1. 应用范围

本文记录 ESP32-S31 控制约 1 万、5 万和 10 万 rpm 高速无刷风机时，三霍尔、反电动势过零检测和无感 FOC 观测器三种方案的差异。

高速风机通常具有以下特点：

- 单方向连续运行；
- 启动后长期工作在中高速区；
- 风阻转矩随转速显著上升；
- 对成本、噪声、效率和最高转速较敏感；
- 零速保持转矩通常不是主要目标。

这些特点使高速风机比舵机更适合无位置传感器控制。

## 2. 三种位置检测与控制方案

| 方案 | 实现难度 | 静止启动 | 高速运行 | 低噪声与平顺性 | 典型用途 |
|---|---:|---:|---:|---:|---|
| 三霍尔六步换相 | 低 | 好 | 中等 | 中等 | 电机已有霍尔、快速开发 |
| 反电动势过零六步换相 | 中等 | 需要开环启动 | 好 | 中等 | 低成本高速风机 |
| 无感 FOC 观测器 | 高 | 需要开环启动 | 好 | 好 | 高效率、低噪声高速风机 |

### 2.1 三霍尔

优点：

- 换相逻辑简单；
- 静止时仍能获得转子扇区；
- 启动和堵转恢复容易；
- 软件开发风险最低。

限制：

- 普通三霍尔每个电周期通常只有 6 个状态区间，角度分辨率约 60° 电角度；
- 高速时需要考虑霍尔器件带宽、传播延迟和安装角度误差；
- 六步换相转矩脉动和声学噪声通常高于 FOC；
- 小型高速风机电机可能没有安装霍尔的位置或成本空间。

如果电机已经带有位置正确、带宽满足要求的霍尔，三霍尔是最快实现方案；如果电机本身没有霍尔，为高速风机增加霍尔未必比无感方案简单。

### 2.2 反电动势过零检测

反电动势过零检测属于无感六步换相，不等同于完整的 FOC 状态观测器。

六步驱动时，每个扇区有一相悬空。控制器检测悬空相反电动势跨过中性点参考电压的时刻，再延迟约 30° 电角度执行下一次换相。

优点：

- 不需要转子传感器；
- 在中高速区反电动势幅值较大，检测可靠；
- 算法和运算量明显低于无感 FOC；
- 是高速风机中常用的低成本方案。

限制：

- 静止和极低速时没有足够的反电动势；
- 必须先进行转子定位和开环升速，再切换到闭环；
- PWM 开关尖峰会干扰过零检测，需要消隐、滤波和迟滞；
- 换相提前角应随转速和负载调整，否则高速效率和最高转速会下降。

### 2.3 无感 FOC 观测器

常见实现包括滑模观测器、磁链观测器、反电动势观测器加 PLL 等。

无感 FOC 观测器通常使用：

- 两相电流采样；
- 直流母线电压；
- PWM 占空比重构的定子电压；
- 电机电阻、电感和磁链参数或其在线估计值。

观测器根据电流和估算电压得到反电动势、磁链或转子电角度。它通常不需要直接测量三根电机相线的瞬时 PWM 电压。

优点：

- 正弦电流，转矩脉动较小；
- 声学噪声通常低于六步换相；
- 可以实现较精细的电流、转矩和弱磁控制；
- 在高转速、反电动势充足时观测效果较好。

限制：

- 算法、标定和调试复杂；
- 对电流采样时刻、偏置、噪声和电机参数敏感；
- 静止时仍不能仅靠反电动势确定转子角度；
- 开环启动到闭环观测的切换必须平滑；
- 高频 PWM 下留给每次控制迭代的时间较短。

## 3. IO 与 ADC 相电压检测

### 3.1 禁止相线直接连接芯片引脚

电机相线存在母线电压、负压尖峰、高 `dv/dt` 和振铃，不能直接连接 ESP32-S31 的 GPIO、ADC 或片内比较器输入。

检测前端至少需要：

- 满足耐压和功耗要求的高阻分压；
- RC 滤波；
- 输入限流；
- 钳位或过压保护；
- 合理的模拟地和功率地布线；
- 必要时增加外部比较器、施密特迟滞或隔离。

### 3.2 比较器输出接 GPIO

推荐的反电动势六步方案为：

```text
三相电压分压 → RC/钳位 → 比较器 → ESP32-S31 GPIO/捕获输入
                         ↑
                    中性点参考
```

特点：

- 比较器完成模拟过零判断；
- MCU 接收整形后的数字边沿；
- 延迟较小，CPU 负担低；
- 容易利用定时器捕获过零时刻；
- 适合 5 万～10 万 rpm 的高速六步控制。

ESP32-S31 数据手册列出一个带 1 路参考输入和 3 路比较输入的模拟比较器资源，但它是按硬件时钟采样的比较器，并非连续时间比较器。正式设计前必须确认其采样时钟、扫描时序、输入范围、迟滞、传播延迟、GPIO/ETM 路由及最新版驱动支持，不能未经带宽和延迟验证就将它作为高速外部比较器的直接替代品。高压、高噪声功率级仍可优先使用经过验证的外部比较器或带反电动势检测功能的栅极驱动器。

### 3.3 分压后接 ADC

ADC 可以采集三相电压并在软件中判断过零，优点是阈值、滤波和诊断灵活，但实现难度更高：

- 必须与 PWM 同步采样；
- 只在开关噪声较小的窗口读取；
- 换相和开关沿之后需要设置消隐时间；
- 需要数字滤波且不能引入过大的相位延迟；
- 会占用本可用于电流采样的 ADC 带宽；
- 高速时每个电角扇区很短，软件和采样延迟直接形成换相角度误差。

ADC 方案适合需要波形诊断或软件算法灵活性的系统。单纯追求容易实现和低延迟时，外部比较器输出接 GPIO 通常更直接。

### 3.4 分压后直接接普通 GPIO

不推荐依靠普通 GPIO 的高低电平阈值直接判断反电动势过零。GPIO 门限、迟滞、温漂和批次差异通常不适合作为精密换相基准。即使进行了分压，也应先经过比较器或可靠的施密特整形。

## 4. 转速、极对数与电角频率

电角频率计算公式：

```text
电角频率(Hz) = 机械转速(rpm) / 60 × 电机极对数
```

六步换相事件频率：

```text
换相事件频率(次/秒) = 电角频率 × 6
```

示例：

| 转速 | 极对数 | 电角频率 | 六步换相事件频率 | 每个扇区时间 |
|---:|---:|---:|---:|---:|
| 10,000 rpm | 1 | 166.7 Hz | 1 kHz | 1 ms |
| 10,000 rpm | 2 | 333.3 Hz | 2 kHz | 500 μs |
| 50,000 rpm | 1 | 833.3 Hz | 5 kHz | 200 μs |
| 50,000 rpm | 2 | 1666.7 Hz | 10 kHz | 100 μs |
| 100,000 rpm | 1 | 1666.7 Hz | 10 kHz | 100 μs |
| 100,000 rpm | 2 | 3333.3 Hz | 20 kHz | 50 μs |

因此只说明机械转速不足以确定控制难度，还必须知道电机极数。10 万 rpm、2 对极时，一个 20 kHz PWM 周期已经等于一个六步扇区时间，20 kHz 不足以提供高质量 FOC 的角度和电流更新分辨率。

无感 FOC 通常希望每个电周期至少有约 10～20 次有效控制更新。以 10 万 rpm、2 对极为例，可能需要约 40～100 kHz 的 PWM/电流环频率，最终值取决于控制算法、功率器件、母线电压和允许的开关损耗。

提高 PWM 频率会同时带来：

- MOSFET 开关损耗增加；
- 栅极驱动损耗增加；
- 死区占比增加；
- ADC 采样窗口缩短；
- MCU 每次控制计算可用时间减少。

因此不能只为提高控制点数无限提高 PWM 频率。

## 5. 反电动势六步控制流程

推荐状态机：

1. **停机检查**
   检查母线电压、功率级故障、过温和转子是否正在随风旋转。

2. **转子定位**
   以受限电流固定转子到已知扇区，定位时间和电流必须限制。

3. **开环换相加速**
   按预定斜坡逐步缩短换相周期，同时限制启动电流。

4. **反电动势有效判定**
   连续检测到合理顺序和间隔的过零事件后，确认转速已进入可观测区。

5. **平滑切入闭环**
   使用当前开环角度和实测过零时刻对齐，避免换相周期突变。

6. **闭环运行**
   根据过零间隔估算速度，并补偿 30° 延时、比较器延迟和高速换相提前角。

7. **失步恢复**
   发现过零丢失、顺序错误或周期突变时立即限流，降速重新捕获或停机。

高速风机可能在上电时被气流带动。应增加顺风、逆风和静止三种启动判定，避免在未知转向下直接强制换相。

## 6. 过零检测时序要点

- 每次 PWM 开关沿和六步换相后设置消隐时间。
- 只检测当前悬空相，屏蔽另外两相。
- 过零事件间隔必须满足合理速度范围。
- 使用连续多次有效事件确认闭环，避免单个噪声边沿切换状态。
- 比较器和滤波器传播延迟应换算为电角度并补偿。
- 高速时应加入可调换相提前角，以补偿绕组电感造成的电流滞后。
- 避免过度 RC 滤波；滤波延迟在 10 万 rpm 时可能形成明显的换相角误差。

## 7. ESP32-S31 方案选择建议

### 电机已有霍尔

优先使用霍尔完成首版：

- 启动简单；
- 容易验证功率级和转向；
- 可快速完成速度闭环；
- 后续可加入相电流采样和换相提前角。

如果最终噪声或效率不能满足要求，再迁移到 FOC。

### 电机没有霍尔，目标是快速实现

优先采用：

```text
三相反电动势分压 + 外部比较器 + GPIO/捕获 + 六步换相
```

这个方案比三相 ADC 软件过零或完整 FOC 观测器容易，尤其适合 5 万～10 万 rpm 的单方向风机。

### 追求低噪声和高效率

采用：

```text
两相电流 ADC + 母线电压 + SVPWM + 滑模/磁链观测器 + PLL
```

可保留反电动势比较器作为启动切换、失步检测或故障诊断的辅助信号。

## 8. 按转速的初步建议

- **约 1 万 rpm**：霍尔和无感都容易实现。已有霍尔时优先霍尔；无霍尔时可使用反电动势过零。
- **约 5 万 rpm**：反电动势较强，比较器过零六步方案通常具有较好的成本和实现难度平衡。
- **约 10 万 rpm**：优先使用低延迟比较器过零或经过验证的高频无感 FOC；必须结合极对数核算电角频率、PWM 频率、ADC 时序和开关损耗。

最终选型不是由机械转速单独决定，还取决于：

- 极对数；
- 母线电压和反电动势常数；
- 绕组电阻、电感；
- 最高负载和转动惯量；
- 声学噪声目标；
- MOSFET 和驱动器开关能力；
- 是否需要弱磁超速；
- 启动可靠性要求。

## 9. 保护与机械安全

高速风机必须具备：

- 硬件过流关断；
- 母线过压和欠压保护；
- 堵转与启动超时保护；
- 过零丢失和换相顺序错误保护；
- 超速保护；
- MOSFET、驱动器和电机过温保护；
- 转子失步后的立即限流或停机；
- 看门狗和通信失效停机；
- 转子动平衡、轴承极限和叶轮爆裂防护。

10 万 rpm 的转子机械风险很高。控制软件验证不能替代转子强度、动平衡、轴承寿命和防爆壳体评估。

## 10. 最终结论

- **实现最简单**：电机已有霍尔时使用三霍尔六步换相。
- **无霍尔条件下最容易实现**：三相分压、比较器整形、GPIO/捕获过零检测和六步换相。
- **综合性能最好但最复杂**：两相电流采样的无感 FOC 观测器。
- 普通 GPIO 不应直接承担模拟过零判断，相线也不能直接连接 ESP32-S31。
- 反电动势方案静止时不可观测，必须设计定位、开环升速和闭环切换状态机。
- 10 万 rpm 设计必须按极对数计算电角频率，不能沿用低速电机的固定 PWM 和采样参数。

## 11. 官方参考资料

- [ESP32-S31 Series Datasheet（预发布）](https://documentation.espressif.com/esp32-s31_datasheet_en.pdf)
- [ESP32-S31 MCPWM 编程指南](https://docs.espressif.com/projects/esp-idf/en/latest/esp32s31/api-reference/peripherals/mcpwm.html)
- [ESP32-S31 ADC 编程指南](https://docs.espressif.com/projects/esp-idf/en/latest/esp32s31/api-reference/peripherals/adc/index.html)
- [ESP32-S31 模拟比较器编程指南](https://docs.espressif.com/projects/esp-idf/en/latest/esp32s31/api-reference/peripherals/ana_cmpr.html)
- [ESP32-S31 ETM 编程指南](https://docs.espressif.com/projects/esp-idf/en/latest/esp32s31/api-reference/peripherals/etm.html)
