# 三轴直流无刷电机控制方案记录

## 方案概述

- 主控：HPM6284（具体料号及封装后续确认）
- 电机数量：3 路直流无刷电机
- 功率驱动：每路使用 1 颗 MP6541A
- 控制算法：编码器 FOC，采用相电流采样
- 电机侧反馈：正交编码器，用于电角度和电机速度闭环
- 减速箱：减速比约 150～200
- 负载侧反馈：SPI 磁编码器，用于减速箱输出端位置闭环
- 参考方案：已有 ESP32-S3 方案，控制算法和状态机可参考，底层外设驱动需按 HPM6284 重写

## 已确认的 ADC 分配

每个电机的 A、B、C 三相电流检测信号分别连接独立 ADC IO，不复用模拟输入：

| 电机 | A 相 | B 相 | C 相 |
| --- | --- | --- | --- |
| M1 | ADC IO 独立通道 | ADC IO 独立通道 | ADC IO 独立通道 |
| M2 | ADC IO 独立通道 | ADC IO 独立通道 | ADC IO 独立通道 |
| M3 | ADC IO 独立通道 | ADC IO 独立通道 | ADC IO 独立通道 |

合计使用 9 路相电流 ADC IO。FOC 每个控制周期根据 PWM 扇区和有效采样窗口选取两相有效电流，第三相用于校验、采样窗口补偿和故障诊断。ADC转换单元可按电机分组，通过 PWM 硬件触发完成同步序列采样。

## 系统与双核架构

推荐使用 HPM SDK、FreeRTOS 和双核 AMP（非 SMP）：

- 电机控制核：裸机运行三路 FOC、速度环和负载侧位置环，不运行通用 RTOS 任务。
- 应用通信核：运行 FreeRTOS，处理 CAN-FD、日志、显示、人机交互、参数管理和升级等功能。
- 两个核心通过共享内存环形队列及硬件邮箱通信。实时状态采用固定长度消息，不在高频控制路径中使用同步 RPC。

这种划分可隔离 CAN-FD 突发中断、显示刷新和日志格式化带来的抖动，同时便于复用 ESP32-S3 方案中的 FreeRTOS 任务、队列和业务状态机。HPM SDK 已提供 FreeRTOS、CAN-FD、FOC及双核通信示例，可作为底层实现基础。

### 任务与中断建议

| 功能 | 运行位置 | 建议周期或方式 |
| --- | --- | --- |
| 硬件过流及PWM关断 | 电机控制核最高优先级中断 | 异步硬件触发 |
| 三路ADC/PWM和FOC | 电机控制核中断 | 20 kHz |
| 电机速度环 | 电机控制核 | 1～2 kHz |
| SPI磁编与输出位置环 | 电机控制核，SPI使用DMA | 200～500 Hz |
| 轨迹及目标值下发 | 应用通信核任务 | 0.5～1 kHz |
| CAN-FD收发 | 应用通信核高优先级任务 | 中断加队列 |
| 故障管理与状态机 | 应用通信核高优先级任务 | 1 kHz或事件触发 |
| 日志输出 | 应用通信核低优先级任务 | 异步DMA |
| 显示刷新 | 应用通信核低优先级任务 | 20～30 Hz |
| 参数存储 | 应用通信核最低优先级任务 | 停机或延迟写入 |

电流环中断禁止执行 `printf`、动态内存分配、Flash写入和阻塞式通信。日志使用定长二进制事件写入无锁环形缓冲区，由低优先级任务异步格式化和输出；缓冲区满时丢弃低等级日志，不能阻塞电机控制。

不优先选择纯裸机承载全部功能，否则CAN-FD、显示和日志扩展后的调度及维护成本较高。RT-Thread也能支持HPM6200，但现有ESP32-S3代码和HPM官方双核电机示例均更适合FreeRTOS路线。

## 推荐控制环路

1. `Id/Iq` 电流环：建议与中心对齐 PWM 同步，初始频率 20 kHz。
2. 电机侧速度环：使用正交编码器，建议 1～2 kHz。
3. 负载侧位置环：使用 SPI 磁编码器，建议 200～500 Hz。
4. 负载位置环输出电机速度或转矩指令，不设置两个并列的位置 PID。
5. 使用电机角度除以减速比进行高频估计，负载侧磁编码器修正齿隙、弹性和累计位置误差。

## 当前风险与约束

- MP6541A 的 SOA/SOB/SOC 仅在对应低侧 MOSFET 电流可观测时有效，采样时刻必须结合 SVPWM 状态选择。
- MP6541A 内部电流检测比例及零点偏差较大，需要上电零偏校准，精密转矩控制还需要逐相增益校准。
- 150～200 减速比会放大齿隙、摩擦和弹性影响，输出位置环需要死区、积分限幅及抗振处理。
- 增量正交编码器上电时没有绝对电角度，需要 Z 相、霍尔、绝对位置来源或受控转子对齐流程。
- SPI 磁编码器需要校验位或 CRC、超时和失联处理；远距离连接时需要评估信号完整性。
- 需要独立的软件限流、硬件快速关断、母线过压及再生能量处理，不能只依赖 MP6541A 内部 OCP。

## 待确认参数

- 母线电压
- 电机额定电流和峰值电流
- 电机极对数、相电阻、相电感及最高转速
- 正交编码器分辨率及是否带 Z 相
- SPI 磁编码器型号、分辨率、是否单圈及连接距离
- 负载惯量、允许齿隙和目标位置精度
- HPM6284 完整订货型号及封装，以完成 PWM、QEI、SPI 和 ADC PinMux 校核
